const gameBoard = (() => {
  const board = Array(9)
  return { board }
})()

const displayController = ((doc) => {
  const display = (array) => {
    array.forEach((element) => {})
  }
  return { display }
})(document)

const players = () => {}
